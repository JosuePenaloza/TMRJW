<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="TMRJW.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TMRJW"
        Title="Navegador" Height="680" Width="1100" MinWidth="900" MinHeight="520">
    <Window.Resources>
        <!-- Plantilla única para miniaturas (soporta CachedImage y BitmapImage) -->
        <DataTemplate x:Key="ThumbnailTemplate">
            <Border Margin="6" Background="White" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1"
                    Padding="6" MinWidth="120" MinHeight="150"
                    Width="{Binding RelativeSource={RelativeSource AncestorType=WrapPanel}, Path=ItemWidth}">
                <Grid>
                    <Image Stretch="UniformToFill" Height="110" VerticalAlignment="Top">
                        <Image.Source>
                            <PriorityBinding>
                                <Binding Path="Image" />
                                <Binding Path="Thumbnail" />
                                <Binding /> <!-- si el item es BitmapImage o string -->
                            </PriorityBinding>
                        </Image.Source>
                    </Image>

                    <!-- Botón eliminar (aparece solo en hover sobre la miniatura) -->
                    <Button Width="30" Height="30"
                            Content="🗑"
                            ToolTip="Eliminar imagen"
                            Click="BtnDeleteImage_Click"
                            DataContext="{Binding}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,6,10,0"
                            Panel.ZIndex="2">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Opacity" Value="0" />
                                <Setter Property="Background" Value="#E53935" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="BorderBrush" Value="#B71C1C" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                        <Setter Property="Opacity" Value="1" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Plantilla para listado vertical (lista de 'Todas' o carpetas) -->
        <DataTemplate x:Key="ListItemTemplate">
            <Border Margin="6" Background="White" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1" Padding="6" Height="100">
                <DockPanel>
                    <Image Width="160" Height="90" Stretch="UniformToFill" DockPanel.Dock="Left" Margin="0,0,8,0">
                        <Image.Source>
                            <PriorityBinding>
                                <Binding Path="Image" />
                                <Binding Path="Thumbnail" />
                                <Binding />
                            </PriorityBinding>
                        </Image.Source>
                    </Image>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding Path=FileName, FallbackValue='Imagen'}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Path=FilePath, FallbackValue=''}" FontSize="11" Foreground="#666" />
                    </StackPanel>
                    <Button Content="🗑" Width="30" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" DockPanel.Dock="Right" Click="BtnDeleteImage_Click" DataContext="{Binding}"/>
                </DockPanel>
            </Border>
        </DataTemplate>

        <!-- Plantilla específica para elementos de vídeo en listas verticales -->
        <DataTemplate x:Key="VideoItemTemplate">
            <Border Margin="4" Background="White" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1" Padding="6" Height="100">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="40" />
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Width="120" Height="80" Stretch="UniformToFill" Margin="0,0,8,0">
                        <Image.Source>
                            <PriorityBinding>
                                <Binding Path="Thumbnail" />
                                <Binding Path="Image" />
                                <Binding />
                            </PriorityBinding>
                        </Image.Source>
                    </Image>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎬" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding Path=FileName}" FontWeight="Bold" TextWrapping="Wrap" />
                        </StackPanel>
                        <!-- file path intentionally hidden to save space -->
                    </StackPanel>

                    <Button Grid.Column="2" Content="🗑" Width="30" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Click="BtnDeleteImage_Click" DataContext="{Binding}"/>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Plantilla específica para elementos de audio en listas verticales (responsive al contenedor) -->
        <DataTemplate x:Key="AudioItemTemplate">
            <Border Margin="4" Background="White" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1" Padding="6" Height="80" HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" /> <!-- icon -->
                        <ColumnDefinition Width="*" />  <!-- título flexible -->
                        <ColumnDefinition Width="Auto" /> <!-- botón eliminar alineado a la derecha -->
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="🎵" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                        <TextBlock Text="{Binding Path=FileName}" FontWeight="Bold" TextWrapping="Wrap" />
                    </StackPanel>

                    <Button Grid.Column="2" Content="🗑" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BtnDeleteImage_Click" DataContext="{Binding}"/>
                </Grid>
            </Border>
        </DataTemplate>

    </Window.Resources>

    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.55*" />
            <ColumnDefinition Width="0.45*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Navegador (sin cambios) -->
        <Grid x:Name="PrincipalNavegacion" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="4" LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Button x:Name="BtnBack" Width="28" Height="24" ToolTip="Atrás"
                            Click="BtnBack_Click" Margin="0,0,6,0" FontSize="14" Content="◀"/>
                    <Button x:Name="BtnForward" Width="28" Height="24" ToolTip="Adelante"
                            Click="BtnForward_Click" FontSize="14" Content="▶"/>
                </StackPanel>

                <TextBox x:Name="UrlBox" Margin="0" Height="28" FontSize="13"
                         HorizontalContentAlignment="Left" Padding="6" Width="188"/>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="6,0,0,0">
                    <Button x:Name="BtnGo" Content="Ir" Width="44" Height="24" Margin="0,0,6,0" Click="BtnGo_Click"/>
                    <Button x:Name="BtnLoadEpubNav" Content="📚 EPUB" Width="74" Height="24" Margin="0,0,6,0" Click="BtnLoadEpubNav_Click" ToolTip="Cargar EPUB y crear pestaña con sus imágenes" />
                    <Button x:Name="BtnRefresh" Content="Actualizar" Background="#FF4D4D" Foreground="White" Width="80" Height="28" Click="BtnRefreshImages_Click" Margin="0,0,6,0" Visibility="Collapsed"/>
                    <!-- <Button x:Name="BtnOpenAjustes" Content="⚙" Background="#1976D2" Foreground="White" Width="36" Height="28" Margin="6,0,0,0" Click="BtnOpenAjustes_Click" ToolTip="Ajustes" /> -->
                    <!-- Indicador pequeño mientras se analizan/descargan imágenes -->
                    <ProgressBar x:Name="CaptureIndicator" Width="18" Height="18" Margin="6,5,0,0" IsIndeterminate="True" Visibility="Collapsed" />
                </StackPanel>
            </DockPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1">
                <WebBrowser x:Name="WebBrowserControl" />
            </Border>
        </Grid>

        <!-- RIGHT: Panel de inventario / previsualización -->
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="0.53*"/>
                <RowDefinition Height="0.47*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,4,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" >
                    <!-- Orden: Proyección (izquierda), Ajustes (centro), Cargar archivos (derecha) -->
                    <Button x:Name="BtnProjectionPower" Content="⏻" Background="#FF8C00" Foreground="White" Height="30" Width="36"
                            Click="BtnProjectionPower_Click" Margin="0,0,6,0" FontSize="16" ToolTip="Encender/Apagar proyección"/>
                    <Button x:Name="BtnOpenAjustesSmall" Content="⚙" Background="#1976D2" Foreground="White" Height="30" Width="36"
                            Click="BtnOpenAjustes_Click" Margin="0,0,6,0" ToolTip="Ajustes"/>
                    <Button x:Name="BtnLoadExtra" Content="📁" Background="#8E24AA" Foreground="White" Height="30" Width="36"
                            Click="BtnLoadExtra_Click" Margin="0,0,6,0" ToolTip="Cargar imágenes / vídeos"/>
                </StackPanel>

                <Button Grid.Column="2" x:Name="BtnTextoDelAnio" Content="Texto del Año" Background="#2E7D32" Foreground="White"
                        Height="32" MinWidth="140" Click="BtnTextoDelAnio_Click" ToolTip="Proyectar Texto del Año" HorizontalAlignment="Right"/>
            </Grid>

            <!-- Pestañas del inventario: aquí se crean dinámicamente desde code-behind -->
            <TabControl x:Name="InventoryTabs" Grid.Row="1" Grid.RowSpan="2" Margin="0,0,0,6" />

            <!-- Preview y controles (sin cambios) -->
            <Border Grid.Row="3" Background="#E8F7FF" BorderBrush="#BEE6FF" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0,8,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" x:Name="PreviewContainer">
                        <Image x:Name="PreviewImage" Stretch="Uniform" Visibility="Visible" />
                        <MediaElement x:Name="PreviewMedia" LoadedBehavior="Manual" Visibility="Collapsed" />
                    </Grid>

                    <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Stretch" Margin="0,8,0,0">
                        <!-- Controles exclusivos para la sección 'Videos Extras' (ubicados encima del nombre/info del elemento) -->
                        <StackPanel x:Name="VideosExtrasControlsPanel" Orientation="Vertical" Visibility="Collapsed" Margin="0,0,0,6" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button x:Name="BtnVideosPlayPause" Content="⏵" Width="44" Height="28" Click="BtnVideosPlayPause_Click" Margin="0,0,6,0" ToolTip="Reproducir/Pausar vídeo seleccionado (Videos Extras)" Background="#2E7D32" Foreground="White" />
                                <Button x:Name="BtnVideosStop" Content="⏹" Width="44" Height="28" Click="BtnVideosStop_Click" Margin="0,0,6,0" ToolTip="Detener reproducción en proyección" Background="#B71C1C" Foreground="White" />
                                <TextBlock Text="" Width="12" />
                                <TextBlock Text="Vol" VerticalAlignment="Center" Margin="6,0,4,0" />
                                <Slider x:Name="VideosVolumeSld" Width="120" Minimum="0" Maximum="100" ValueChanged="VideosVolumeSld_ValueChanged" Value="75" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                                <Slider x:Name="VideosSldTimeline" Minimum="0" Maximum="1" Value="0" Width="260" PreviewMouseDown="VideosSldTimeline_PreviewMouseDown" PreviewMouseUp="VideosSldTimeline_PreviewMouseUp" ValueChanged="VideosSldTimeline_ValueChanged" />
                                <TextBlock x:Name="TxtVideosCurrentTime" Text="00:00:00" VerticalAlignment="Center" Margin="6,0,6,0" />
                                <TextBlock x:Name="TxtVideosTotalTime" Text="/00:00:00" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>

                        <!-- Nombre del elemento (arriba) -->
                        <TextBlock x:Name="TxtPreviewInfo" Text="Imagen / Video" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,6" />

                        <!-- Multimedia buttons: directly under the name, left-aligned -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,6">
                            <StackPanel Orientation="Horizontal" x:Name="PreviewMediaControls" Visibility="Collapsed" HorizontalAlignment="Left">
                                <Button x:Name="BtnPreviewPrev" Content="⏮" Width="36" Height="28" Click="BtnPreviewPrev_Click" Margin="0,0,6,0" ToolTip="Anterior"/>
                                <Button x:Name="BtnPreviewPlayPause" Content="⏵" Width="36" Height="28" Click="BtnPreviewPlayPause_Click" Margin="0,0,6,0"/>
                                <Button x:Name="BtnPreviewStop" Content="⏹" Width="36" Height="28" Click="BtnPreviewStop_Click" Margin="0,0,6,0"/>
                                <Button x:Name="BtnPreviewNext" Content="⏭" Width="36" Height="28" Click="BtnPreviewNext_Click" Margin="0,0,6,0" ToolTip="Siguiente"/>
                                <Button x:Name="BtnShuffle" Content="🔀" Width="36" Height="28" Click="BtnShuffle_Click" Margin="0,0,6,0" ToolTip="Reproducción aleatoria"/>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                                    <TextBlock Text="Vol" VerticalAlignment="Center" Margin="6,0,4,0" />
                                    <Slider x:Name="PreviewVolumeSld" Width="90" Minimum="0" Maximum="100" ValueChanged="PreviewVolumeSld_ValueChanged" Value="75" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>

                        <!-- Timeline debajo de los botones -->
                        <StackPanel x:Name="PreviewTimelinePanel" Orientation="Horizontal" Margin="0,4,0,0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Visibility="Collapsed">
                            <Slider x:Name="PreviewSldTimeline" Minimum="0" Maximum="1" Value="0" Width="220" Margin="0,0,8,0" PreviewMouseDown="PreviewSldTimeline_PreviewMouseDown" PreviewMouseUp="PreviewSldTimeline_PreviewMouseUp" ValueChanged="PreviewSldTimeline_ValueChanged" />
                            <TextBlock x:Name="TxtPreviewCurrentTime" Text="00:00:00" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="TxtPreviewTotalTime" Text="/00:00:00" VerticalAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Botones de zoom/pan/reset (sin cambios) -->
            <Border Grid.Row="4" Background="#FFFFFF" CornerRadius="4" BorderBrush="#C7A77B" BorderThickness="1" Padding="6" Margin="0,0,0,4">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,0" x:Name="ZoomButtonsPanel" VerticalAlignment="Center">
                        <Button x:Name="BtnPanUp" Content="▲" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnPanUp_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnPanLeft" Content="◀" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnPanLeft_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnZoomIn" Content="+" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnZoomIn_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnZoomOut" Content="–" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnZoomOut_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnPanRight" Content="▶" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnPanRight_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnPanDown" Content="▼" Width="36" Height="28" Background="#A67C52" Foreground="White" Click="BtnPanDown_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnResetZoom" Content="Reset" Width="60" Height="28" Background="#A67C52" Foreground="White" Click="BtnResetZoom_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>